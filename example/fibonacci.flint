@external(c, "flint_stdlib", "print")
pub fun print(string: String) Nil

@external(c, "flint_stdlib", "to_string")
pub fun to_string(int: Int) String

fun fib(n: Int) Int {
    fun aux(m: Int, a: Int, b: Int) Int {
        match m {
            | 0 -> a
            | _ -> aux(m - 1, b, a + b)
        }
    }
    aux(n, 0, 1)
}

pub fun main() Nil {
    print(to_string(fib(10)))
    print("\n")
}